#include<bits/stdc++.h>

using namespace std;

// Pair is present inside the utility library

int main() {
    pair<int, int> p = {1,3};
    cout<<p.first<<" "<<c.second<<endl;

    pair<int, pair<int, int>> p = {1, {3,4}};
    count<<p.first<<" "<<p.second.first<<" "<<p.second.second<<endl;

    pair<int, int> arr[] = { {1,2}, {2,5}, {5,6}};
    cout<<arr[1].second;

    return 0;
}